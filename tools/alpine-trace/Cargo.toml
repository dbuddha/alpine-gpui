[package]
name = "alpine-trace"
description = "Non-shipping decoder for versioned Alpine renderer workloads"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish.workspace = true
license-file.workspace = true

[dependencies]
alpine-core = { path = "../../crates/alpine-core", version = "=0.0.0" }
alpine-metal = { path = "../../crates/alpine-metal", version = "=0.0.0" }
alpine-scene = { path = "../../crates/alpine-scene", version = "=0.0.0" }

[lints]
workspace = true
