[package]
name = "alpine-scene"
version = "0.0.0"
description = "Immutable backend-neutral scene protocol for Alpine GPUI"
edition.workspace = true
rust-version.workspace = true
publish.workspace = true

[dependencies]
alpine-core = { path = "../alpine-core" }

[lints]
workspace = true
